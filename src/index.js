import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import App from './App'
// to move line in vis studio use alt + up or down key

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>, 
    document.getElementById('root')
)
// make sure every page has blank line at the end or
// at the end of every code block
